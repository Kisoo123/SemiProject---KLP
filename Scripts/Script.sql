SELECT * from artist_group;
SELECT *FROM MEMBER;

ALTER TABLE MEMBER ADD(GROUP_NO NUMBER REFERENCES ARTIST_GROUP(GROUP_NO));

SELECT * FROM (SELECT ROWNUM AS RNUM, N.* FROM (SELECT * FROM MEMBER LEFT JOIN ARTIST_GROUP USING(GROUP_NO) ORDER BY ENROLL_DATE DESC)N) WHERE RNUM BETWEEN 1 AND 5